import { Placeholder } from '../components/Placeholder'
export function Signals() {
  return (
    <Placeholder title="Signals" step={7}>
      every candidate, including rejections with the gate that killed it.
    </Placeholder>
  )
}

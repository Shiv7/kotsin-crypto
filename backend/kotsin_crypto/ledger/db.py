"""Step 6 — SQLAlchemy async models over SQLite (WAL): control (mode, armed_until, halted), signals
(incl. rejections + failing gate), orders, fills, positions, trades (R-multiple, MFE/MAE, fees,
funding), wallet_snapshots. One writer: the engine."""

"""Step 6 — the ONLY module that decides an exit (LEARNINGS R11): initial stop, ratchet ladder,
time-stop, funding-aware flatten, all evaluated on mark price. Emits ORDER_INTENT; never touches the
venue."""

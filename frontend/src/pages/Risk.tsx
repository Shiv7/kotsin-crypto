import { Placeholder } from '../components/Placeholder'
export function Risk() {
  return (
    <Placeholder title="Risk" step={7}>
      exposure by underlying, breakers, and the Halt button (with confirm).
    </Placeholder>
  )
}
